bup-service:
  testing: true

eureka:
  client:
    enabled: false
    registerWithEureka: false
  serviceUrl:
    fetchRegistry: false

ailegorreta:
  neo4j:
    flyway:
      locations: /db/neo4jtest/migration # Script to initialize bupDB for testing

logging:
  level:
    root: INFO
    com:
      acme:
        orderservice: DEBUG
      ailegorreta: DEBUG